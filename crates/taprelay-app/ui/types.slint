// Log severity as shown by the log page; TRACE is reported as debug.
export enum LogLevel { debug, info, warning, error }
export struct LogLine { text: string, level: LogLevel }
export struct ShortcutItem { text: string, keys: [string], slot: int, enabled: bool }
export struct FunctionBindingRow {
    id: string,
    label: string,
    activation: string,
    enabled: bool,
    shortcuts: [ShortcutItem]
}
export struct BindingRow { text: string, keys: [string], enabled: bool }
export struct DeviceRow { id: string, name: string, detail: string, selected: bool, action: string, action-label: string, enabled: bool, paired: bool, device-kind: string, status: string }
export struct CheckRow { title: string, state: int }
export struct RelayStatus {
    device-name: string,
    device-selected: bool,
    listening: bool,
    ready: bool,
    show-retry: bool,
    signal: color,
    stage: string,
    input-count: int
}
